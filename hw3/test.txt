class A {
    A () {
        if (z) {
            if (y) {
                return y;
            }
            return z;
        }
        continue;
    }
}

Blockkkk ([
    if(z) Blockqqq ([
        if(y) Blockqqq ([
            return (y) ])  
        ]) if(z) Blockqqq ([
            return (z) 
        ]) 
    continue ()
])